{
  "private": true,
  "scripts": {
    "build": "esbuild main.js --bundle --minify --outfile=../lib/assets/xterm/build/main.js --format=esm --loader:.js=jsx",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.16"
  }
}
